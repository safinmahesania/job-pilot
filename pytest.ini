[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    # Starlette's TestClient and FastAPI's on_event are deprecated upstream.
    # Not ours to fix, and not worth drowning real warnings in.
    ignore::DeprecationWarning
