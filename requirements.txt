# Web backend
fastapi
uvicorn[standard]
pydantic

# Config & HTTP
pyyaml
httpx
python-dotenv

# Scraping / parsing
beautifulsoup4

# File uploads (CSV / Excel import)
python-multipart
openpyxl

# PDF export for generated resumes / cover letters
fpdf2

# Local LLM scoring (requires Ollama running separately)
ollama
python-docx>=1.1
pypdf>=4.0        # tests: read back the PDF we generated, without a system binary
