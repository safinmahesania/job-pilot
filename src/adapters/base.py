"""Abstract SourceAdapter interface; fetch() returns raw job records."""
# TODO: define SourceAdapter (ABC) with fetch() -> list[dict], and a factory
#       that maps an `ats` string to the right adapter class.
