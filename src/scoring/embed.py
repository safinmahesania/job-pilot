"""Embeds job descriptions + profile; ranks by cosine similarity (pgvector)."""
# TODO: embed surviving jobs and the profile; return top-N by cosine distance.
#       Cache embeddings so re-runs are cheap.
